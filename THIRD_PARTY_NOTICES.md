# Third-party notices

This project bundles or vendors the following third-party components in
[`src/assets/`](src/assets). Their licenses are reproduced below or are
included alongside the files.

## PDF.js (`src/assets/pdfjs/`)

Bundled build of **PDF.js 2.2.228**.

- Project: https://mozilla.github.io/pdf.js/
- License: Apache License 2.0
- The full license notice is included at the top of `src/assets/pdfjs/build/pdf.js`.
- The CMap tables in `src/assets/pdfjs/web/cmaps/` are © 1990-2009 Adobe Systems
  Incorporated and are distributed under the license reproduced in
  `src/assets/pdfjs/web/cmaps/LICENSE`.

## CryptoPro CAdES plugin API (`src/assets/cadesplugin_api.js`)

Browser script distributed by **CryptoPro** (https://cryptopro.ru/) that
initializes the `cadesplugin` object used for CAdES operations. It is loaded at
runtime only when the CryptoPro browser extension is installed.

## Icons (`src/assets/imgs/`)

SVG icon assets used by the annotation toolbar. The demo ships a mirror of
these assets under `demo/src/assets/imgs/`.
